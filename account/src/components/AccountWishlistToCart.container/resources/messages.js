import {defineMessages} from 'react-intl';

export default defineMessages({
    success_message: 'Wishlist item(s) added to cart',
    success_message_partial: 'Some wishlist item(s) added to cart'
});
